apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: delta
  namespace: keda113
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: year.alwayslearn.in
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: demoapp-svc
                port:
                  number: 80
